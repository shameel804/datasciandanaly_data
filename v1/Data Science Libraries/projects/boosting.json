[
    {
        "title": "XGBoost Basics 🚀",
        "ques": "Train an **XGBoost** model.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "import xgboost as xgb\n\nmodel = xgb.XGBClassifier(\n    n_estimators=100,\n    max_depth=5,\n    learning_rate=0.1\n)\n\nmodel.fit(X_train, y_train)\npredictions = model.predict(X_test)"
        },
        "explanation": "**XGBoost** is a powerful gradient boosting library."
    },
    {
        "title": "LightGBM Training 💡",
        "ques": "Use **LightGBM**.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "import lightgbm as lgb\n\ntrain_data = lgb.Dataset(X_train, label=y_train)\n\nparams = {\n    'objective': 'binary',\n    'num_leaves': 31\n}\n\nmodel = lgb.train(params, train_data, num_boost_round=100)"
        },
        "explanation": "**LightGBM** is fast and memory-efficient."
    },
    {
        "title": "Feature Importance 📊",
        "ques": "Get **feature importance**.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# XGBoost\nimportances = model.feature_importances_\n\n# Plot\nimport matplotlib.pyplot as plt\nplt.barh(feature_names, importances)\nplt.xlabel('Importance')\nplt.show()"
        },
        "explanation": "**Importance** shows which features matter."
    },
    {
        "title": "Early Stopping ⏸️",
        "ques": "Use **early stopping**.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "model = xgb.XGBClassifier(early_stopping_rounds=10)\n\nmodel.fit(\n    X_train, y_train,\n    eval_set=[(X_val, y_val)],\n    verbose=True\n)"
        },
        "explanation": "**Early stopping** prevents overfitting."
    },
    {
        "title": "CatBoost Categories 🐱",
        "ques": "Handle **categoricals** with CatBoost.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "from catboost import CatBoostClassifier\n\nmodel = CatBoostClassifier(cat_features=['city', 'product'])\nmodel.fit(X_train, y_train)"
        },
        "explanation": "**CatBoost** handles categories natively."
    },
    {
        "title": "GPU Training ⚡",
        "ques": "Train on **GPU**.",
        "answer": {
            "type": "code",
            "lang": "python",
            "content": "# XGBoost\nmodel = xgb.XGBClassifier(tree_method='gpu_hist')\n\n# LightGBM\nparams = {'device': 'gpu'}\n\n# CatBoost\nmodel = CatBoostClassifier(task_type='GPU')"
        },
        "explanation": "**GPU** accelerates training significantly."
    }
]